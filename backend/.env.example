OPENAI_API_KEY=sk-xxxxx
# Only set this if your key is for a non-default endpoint (e.g. Azure AI
# Foundry). Leave unset to hit the standard api.openai.com.
OPENAI_BASE_URL=
OPENAI_MODEL=gpt-5.5
# Optional, Phase 3 stretch only:
WEB_SEARCH_API_KEY=
